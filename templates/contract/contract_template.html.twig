<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>Web Development Service Agreement</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            line-height: 1.1;
            margin: 20px;
        }
        .header {
            text-align: center;
            margin-bottom: 30px;
        }
        .section {
            margin-bottom: 20px;
        }
        .company-info {
            margin-bottom: 30px;
        }
    </style>
</head>
<body>
    <div class="header">
        <h2>WEB DEVELOPMENT SERVICE AGREEMENT</h2>
        <p>Date: {{ date|date('F d, Y') }}</p>
    </div>

    <div class="company-info">
        <h3>SERVICE PROVIDER</h3>
        <p>
            {{ company.name }}<br>
            {{ company.company }}<br>
            SIRET: {{ company.siret }}<br>
            Email: {{ company.email }}
        </p>
    </div>

    <div class="section">
        <h3>CLIENT INFORMATION</h3>
        <p>
            {% if data.clientName is not empty %}Name/Company: {{ data.clientName }}<br>{% endif %}
            {% if data.clientAddress is not empty %}Address: {{ data.clientAddress }}<br>{% endif %}
            {% if data.clientEmail is not empty %}Email: {{ data.clientEmail }}<br>{% endif %}
            {% if data.clientSiret is not empty %}SIRET/Registration: {{ data.clientSiret }}<br>{% endif %}
        </p>
    </div>

    <div class="section">
        <h3>1. PROJECT SCOPE</h3>
        <p><strong>Subject:</strong> {{ data.projectSubject }}</p>
        <p><strong>Selected Plan:</strong> {{ data.selectedPlan }}</p>
        <h4>1.1 Inclusions</h4>
        <p>{{ data.projectInclusions|nl2br }}</p>
        <h4>1.2 Monthly Maintenance</h4>
        <p>Monthly maintenance service is {% if data.monthlyMaintenance %}included{% else %}not included{% endif %} in this agreement.</p>
    </div>

    <div class="section">
        <h3>2. PROJECT TIMELINE</h3>
        <p>
            <strong>Start Date:</strong> {{ data.startDate|date('F d, Y') }}<br>
            <strong>Estimated Duration:</strong> {{ data.estimatedDuration }}<br>
            <strong>Delivery Deadline:</strong> {{ data.deliveryDeadline|date('F d, Y') }}
        </p>
    </div>

    <div class="section">
        <h3>4. PAYMENT TERMS</h3>
        <p>
            <strong>Total Project Cost:</strong> €{{ data.totalPrice }}<br>
            <strong>Payment Method:</strong> {{ data.paymentMethod }}<br>
            <strong>Payment Structure:</strong> {% if data.fullPayment %}100% upfront payment{% else %}Payment in installments{% endif %}
        </p>
        <h4>4.1 Banking Details</h4>
        <p>{{ data.bankDetails|nl2br }}</p>
    </div>

    <div class="section">
        <h3>5. GENERAL TERMS</h3>
        <p>5.1 This agreement is governed by French law.</p>
        <p>5.2 Any modifications to this agreement must be made in writing and agreed upon by both parties.</p>
        <p>5.3 The Service Provider retains intellectual property rights until full payment is received.</p>
        <p>5.4 Confidentiality of business information will be maintained by both parties.</p>
    </div>

    <div>
        <h3>6. SIGNATURES</h3>
        <table style="width: 100%; margin-top: 20px;">
            <tr>
                <td style="width: 50%; vertical-align: top;">
                    <p><strong>Service Provider</strong><br>{{ company.name }}<br>Date: {{ date|date('d/m/Y') }}</p>
                </td>
                <td style="width: 50%; vertical-align: top;">
                    <p><strong>Client</strong><br>{{ data.clientName }}<br>Date: {{ date|date('d/m/Y') }}</p>
                </td>
            </tr>
        </table>
    </div>
</body>
</html>