<div class="container-fluid">

    <div class="row text-center">
        <nav class="mt-5 mb-3">
            <a class="my-5 text-center{{ route == 'mentions_legales' ? ' nav-active' }} linked fw-bold" href="{{ path('mentions_legales') }}">Legal Notices</a>
			{# <p class="text-center text-light fs-6">We stand with Palestine.</p> #}
        </nav>
    </div>

</div>